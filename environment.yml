name: moss
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python>=3.9
  - numpy>=1.24.0
  - scipy>=1.10.0
  - pillow>=9.0.0
  - matplotlib>=3.7.0
  - tqdm>=4.65.0
  - scikit-image>=0.20.0
  - tifffile>=2023.1.0
  - pip
  # PyTorch - conda will select appropriate version for your platform
  - pytorch>=2.0.0
  - torchvision>=0.15.0
  # For Mac with Apple Silicon, pytorch-nightly may be needed for MPS support
  # - pytorch-nightly (uncomment if needed)
  - pip:
    - PyQt6>=6.4.0
    - opencv-python>=4.7.0
    - websockets>=12.0
